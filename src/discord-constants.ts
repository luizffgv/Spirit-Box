/** How many buttons there can be in an action row. */
export const MAX_BUTTONS_PER_ACTION_ROW = 5;

/** How many action rows there can be in a message. */
export const MAX_ACTION_ROWS_PER_MESSAGE = 5;
